spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taskdb
    username: postgres
    password: password
    hikari:
      leak-detection-threshold: 2000
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
